#include "rate_limit.h"

// Implementation of the RateLimit class is in the header file (rate_limit.h) 
// because it's a template class. This file exists primarily for build system integration.

namespace qb::http {
    // Add any non-template utility functions here if needed in the future
} 